import{_ as t,o,c as n,a7 as r,ag as s,ah as a}from"./chunks/framework.CXW4QS8X.js";const m=JSON.parse('{"title":"Fault finding","description":"","frontmatter":{},"headers":[],"relativePath":"guide/fault-finding.md","filePath":"guide/fault-finding.md","lastUpdated":1787138510000}'),i={name:"guide/fault-finding.md"};function l(d,e,c,h,u,p){return o(),n("div",null,[...e[0]||(e[0]=[r('<h1 id="fault-finding" tabindex="-1">Fault finding <a class="header-anchor" href="#fault-finding" aria-label="Permalink to “Fault finding”">​</a></h1><h2 id="startup-process" tabindex="-1">Startup process <a class="header-anchor" href="#startup-process" aria-label="Permalink to “Startup process”">​</a></h2><p>The addon follows the following startup process:</p><ol><li><p>Load config, sensor definitions and schedules</p><p>The logs will show if you use any unknown or deprecated sensors, if you have anything wrong in your custom sensors, and the intended schedule for reading sensors.</p></li><li><p>Connect to the Inverter</p><p>Connect and read the inverter identity (device type, protocol, serial) from registers 0–7, then rated power from the selected sensor definitions. This is the first step to ensure the connection is working.</p><p>If this read fails, you need to follow the fault finding guide below. It could be a cabling issue, a configuration issue, or a hardware issue.</p><p>If successful, the serial number in your config is checked against the inverter. A warning is logged when <code>SENSOR_DEFINITIONS</code> does not match the device type. The log will show identity details and the rated-power startup read.</p><p>If the identity and rated power reads succeed, it will read all configured sensors to ensure you have some values at startup</p></li><li><p>Connect to the MQTT server</p><p>Publish the discovery data for Home Assistant, and also remove discovery data if required</p><p>If this step fails, you will not see any entities in Home Assistant and you need to check your MQTT server settings.</p></li></ol><p>Once the startup is complete, the addon will continue to read &amp; publish sensor data. During this process you will occasionally see read failures. As long as this does not happen on every read, you can probably continue using the addon, but can consider reducing sensors, relaxing the read schedules, etc.</p><p>If you poll several inverters on one RS485 bus and one unit stops replying, it might enter a <strong>stale</strong> state so the bus is not blocked. Tune <code>STALE_INVERTER_AFTER_SECONDS</code> and <code>STALE_INVERTER_SKIP_SECONDS</code> in the Supervisor options (see <a href="./../reference/multi-options#stale-inverter-global">Multi add-on options</a>). After each <strong>successful</strong> Modbus read, a timer is armed: if reads are still failing once that many seconds have passed since the last success, the add-on pauses normal polling for that inverter for the configured skip period, then probes the serial register once before resuming or extending stale quiet.</p><p>A parallel AC setup (inverter <strong>Parallel Mode</strong> Client / Server) does <strong>not</strong> mean both inverters share the RS485 Modbus bus. If the second inverter never appears, see <a href="#parallel-inverters">Parallel inverters</a> below.</p><p>If you fail to get a reply from the inverter, typically if step #2 fails, please check the following:</p><h2 id="a-cabling-connection" tabindex="-1">(A) Cabling &amp; connection <a class="header-anchor" href="#a-cabling-connection" aria-label="Permalink to “(A) Cabling &amp; connection”">​</a></h2><p>While fault finding use as short as possible cable, outside any sprague/trunking. Once everything works, you can switch to a more permanent, much longer cable.</p><p>If you cannot establish a connection, check the RS485 adaptor and cabling. Are you plugged into the correct port, is your connector crimped correctly?</p><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>Many newer Sunsynk/Deye hybrids use one <strong>BMS</strong> RJ45 socket for both <strong>RS485</strong> (what this add-on uses) and <strong>CAN</strong> (battery management). If the battery already uses that port, you cannot simply “share” a normal patch cable—you need a splitter or Y-cable that keeps CAN on its pair and RS485 on <strong>pins 1–3</strong> only on the adaptor leg. Pinout, splitter layouts, and what goes wrong if all eight wires are duplicated to both sides are covered in <strong><a href="./wiring#combined-rs485-can-bms-port">Adaptors &amp; Wiring</a></strong> (continue through <strong>If the battery already uses this port</strong> and <strong>Pin reference</strong>).</p></div><p>Other factors that might impact connection, or reliability:</p><ul><li>Use a RJ45 converter with a GROUND pin. Ensure the ground is connected.</li><li>Re-crimp your RJ45 connector.</li><li>Use a good quality solid CAT5e/CAT6 cable. <ul><li>Ensure the data line is using a twisted pair.</li></ul></li><li>Ensure your RS485 cable does not run parallel to other electrical cables (AC or DC), to reduce interference. e.g. in trunking. <ul><li>If interference is a problem, are you using a twisted pair in the cable?</li><li>If interference is a problem, it could also help to use a shielded cable. Ground the shield at ONE end only (i.e. on the USB adaptor side and then just use normal plastic RJ45 connector on the inverter side.)</li></ul></li><li>If you still fail to make a connection, test the line voltage resistor (see Reducing timeouts below)</li></ul><h2 id="b-configuration" tabindex="-1">(B) Configuration <a class="header-anchor" href="#b-configuration" aria-label="Permalink to “(B) Configuration”">​</a></h2><h3 id="check-the-modbus-server-id" tabindex="-1">Check the Modbus Server ID <a class="header-anchor" href="#check-the-modbus-server-id" aria-label="Permalink to “Check the Modbus Server ID”">​</a></h3><p>Ensure the Modbus Server ID (<code>MODBUS_ID</code> config setting) matches the configured <strong>Modbus SN</strong> value of the inverter. This value must not be zero.</p><p>View/update the Modbus server ID on your inverter under &quot;Advanced Settings&quot; / &quot;Multi-Inverter&quot;.</p><p>Please note that this can be reset to zero after a software upgrade on your inverter, and this will stop the addon from reading data from your inverter. Resetting it to the previous value (the value the value in <code>MODBUS_ID</code> if you had this working previously), and then restarting the inverter should fix the <a href="https://powerforum.co.za/topic/15779-home-assistant-no-longer-getting-data-after-sunsynk-firmware-update-solved/" target="_blank" rel="noreferrer">issue</a>.</p><p><img src="'+s+'" alt="Modbus Server ID" width="70%"></p><h3 id="only-a-single-connection-to-the-serial-port" tabindex="-1">Only a single connection to the serial port <a class="header-anchor" href="#only-a-single-connection-to-the-serial-port" aria-label="Permalink to “Only a single connection to the serial port”">​</a></h3><p>Ensure you only have a single addon connected to the serial port. The following can all potentially access the USB port: mbusd, Node RED, the normal and dev addon version.</p><p>If you need to have multiple connections to the serial port: ONLY connect mbusd to the serial port. Connect all addons to mbusd (e.g. tcp://192.168.1.x:503).</p><h2 id="c-reducing-timeouts" tabindex="-1">(C) Reducing timeouts <a class="header-anchor" href="#c-reducing-timeouts" aria-label="Permalink to “(C) Reducing timeouts”">​</a></h2><p>If you get many timeouts, or if the addon does not read all your sensors on startup (i.e. you see <strong>Retrying individual sensors</strong> in the log), you can try the following. The <strong>RS485 timeout</strong> diagnostic sensor on each inverter (<a href="#stats">Stats</a>) shows whether timeouts are still climbing.</p><ul><li>Set <code>READ_SENSORS_BATCH_SIZE</code> to a smaller value, i.e. 8.</li><li>Direct serial and TCP wait <code>READ_MESSAGE_SPACING</code> seconds after each reply (default <strong>0.05</strong>). Raising <code>TIMEOUT</code> only waits longer for a missing reply; it does not add that gap. Try <strong>0.1</strong> or <strong>0.15</strong> if timeouts continue.</li><li>The most reliable way to connect is to use mbusd to the serial port &amp; connect the addon to mbusd at <code>tcp://&lt;ip&gt;:502</code>. The mbusd instance/addon can be on the same physical device or a remote device.</li></ul><p>Check the cabling and connection again. Use a 1m cable and stand next to the inverter while testing.</p><h3 id="direct-serial" tabindex="-1">Direct serial <a class="header-anchor" href="#direct-serial" aria-label="Permalink to “Direct serial”">​</a></h3><p>If your RS485 adaptor is plugged directly into your host, connecting directly to the serial port <code>PORT: &quot;/usb/ttyX&quot;</code> might not give you the best results.</p><p>Once you have a working connection (reading the serial), consider introducing <strong>mbusd</strong> into your setup, in this configuration mbus connects to the serial port and the addon connects via TCP, typically: <code>PORT: tcp://homeassistant.local:502</code></p><p><strong>mbusd</strong> over <code>tcp://</code> is a reliable setup. Direct serial (<code>PORT: /dev/ttyUSB0</code>) is also supported (tmodbus); see <a href="./../reference/multi-options#port">configuration</a>. If direct serial is flaky, prefer mbusd.</p><h3 id="check-line-voltage-termination-resistor" tabindex="-1">Check line voltage / termination resistor <a class="header-anchor" href="#check-line-voltage-termination-resistor" aria-label="Permalink to “Check line voltage / termination resistor”">​</a></h3><p>If your RS485 adapter has a termination resistor (typically 120 ohms), try removing it.</p><p>To check, disconnect the adapter and use a multimeter to measure the resistance between A &amp; B.</p><p>The d.c. voltage between A/B on the sunsynk RS485 connection should idle around 4-5v with nothing connected, but this may drop to around 0.5v with the 120 ohm load.</p><p>RS485 devices are typically multi-drop with a termination resistor on the first and last devices. However, the RS485 BMS port may only be intended to connect to a single device.</p><p><img src="'+a+'" alt="RS485" width="70%"></p><h2 id="parallel-inverters" tabindex="-1">(D) Parallel inverters — second inverter not responding <a class="header-anchor" href="#parallel-inverters" aria-label="Permalink to “(D) Parallel inverters — second inverter not responding”">​</a></h2><p>The add-on can poll several inverters on <strong>one</strong> adaptor when they share a physical RS485 bus: same <code>PORT</code>, unique <code>MODBUS_ID</code> (and <code>SERIAL_NR</code> / <code>HA_PREFIX</code>) per unit. See <a href="./../reference/multi-options#port">Shared RS485 bus</a>.</p><p>Parallel Mode does <strong>not</strong> automatically daisy-chain Modbus. If only one inverter answers, or the other goes <strong>stale</strong> / startup shows no response, test <strong>one inverter at a time</strong> with the <strong>same</strong> USB adaptor. Change only which RJ45 you plug into and the single <code>INVERTERS</code> entry. The inverter LCD may still say Master / Slave for Parallel Mode; this add-on uses <strong>Client</strong> / <strong>Server</strong>. See <a href="./overview#modbus">Modbus</a>.</p><ol><li><p><strong>Baseline — inverter 1 only.</strong> Plug the cable into inverter 1. Configure one <code>INVERTERS</code> entry for that unit (<code>MODBUS_ID</code> matching its <strong>Modbus SN</strong>). If this fails, it is a normal connection problem — follow (A)–(C).</p></li><li><p><strong>Inverter 2 on its own socket.</strong> Move the <strong>same</strong> cable to inverter 2. Change the config to inverter 2 only (<code>HA_PREFIX</code>, <code>MODBUS_ID</code>, <code>SERIAL_NR</code>). If this fails while plugged into inverter 2, inverter 2 is not answering Modbus: check its Modbus SN, cabling, and whether that model exposes RS485 on the port you are using when Parallel Mode is Server.</p></li><li><p><strong>Is the bus shared?</strong> Plug the cable back into inverter 1. Keep the <strong>inverter 1</strong> config. Then move the cable to inverter 2 <strong>without changing the config</strong>.</p><ul><li>If inverter 1 still answers with the cable on inverter 2, the RS485 bus <strong>is</strong> shared — use the <strong>same</strong> <code>PORT</code> for both <code>INVERTERS</code> entries, with unique <code>MODBUS_ID</code>s.</li><li>If it goes silent, the bus is <strong>not</strong> shared. Use <strong>two adaptors</strong> (two <code>PORT</code>s), one per inverter.</li></ul></li><li><p><strong>Optional cross-check.</strong> Cable on inverter 1, config for inverter 2 only. A reply means inverter 2 is reachable through inverter 1&#39;s socket (shared bus). No reply means the bus is not shared, or inverter 2&#39;s <code>MODBUS_ID</code> is wrong.</p></li></ol><p>When using two adaptors, <code>MODBUS_ID</code> must match whatever unit actually answers on <strong>that</strong> adaptor. Some units in Server mode still answer as <code>1</code> on their own RS485 port even when the display shows a different Modbus SN.</p><h2 id="stats" tabindex="-1">Stats <a class="header-anchor" href="#stats" aria-label="Permalink to “Stats”">​</a></h2><p>Each inverter publishes two <strong>diagnostic</strong> MQTT entities (not listed under <code>SENSORS</code>). They appear on the inverter device in Home Assistant under <strong>Diagnostic</strong>, and update every <strong>120 seconds</strong>.</p><h3 id="rs485-timeout" tabindex="-1">RS485 timeout <a class="header-anchor" href="#rs485-timeout" aria-label="Permalink to “RS485 timeout”">​</a></h3><p>Cumulative Modbus timeout count for that inverter since the add-on started (reads and writes, including gateway <strong>0x0B</strong>). Occasional increases are normal. A steadily rising count means the bus is dropping replies — see <a href="#c-reducing-timeouts">(C) Reducing timeouts</a>.</p><h3 id="callback-stats" tabindex="-1">Callback stats <a class="header-anchor" href="#callback-stats" aria-label="Permalink to “Callback stats”">​</a></h3><p>Duration of that inverter&#39;s poll cycle. The state is the <strong>mean</strong> time in seconds over the last 120s. Attributes add <code>count</code>, <code>min</code>, <code>max</code>, <code>median</code>, <code>stdev</code>, <code>p5</code>, <code>p95</code>, plus:</p><ul><li><code>busy_count</code> — a new poll was skipped because the previous one was still running</li><li><code>error_count</code> — the poll raised an error</li></ul><p>If <strong>mean</strong> / <strong>p95</strong> approach the schedule interval, or <strong>busy_count</strong> climbs, the add-on cannot keep up: reduce sensors, relax schedules, or fix timeouts. <code>error_count</code> should stay near zero.</p>',50)])])}const f=t(i,[["render",l]]);export{m as __pageData,f as default};
