import{_ as e,c as o,o as t,Q as a}from"./chunks/framework.7f5240a6.js";const b=JSON.parse('{"title":"Run standalone using docker compose","description":"","frontmatter":{},"headers":[],"relativePath":"guide/standalone-deployment.md","lastUpdated":1689771178000}'),s={name:"guide/standalone-deployment.md"},n=a('<h1 id="run-standalone-using-docker-compose" tabindex="-1">Run standalone using docker compose <a class="header-anchor" href="#run-standalone-using-docker-compose" aria-label="Permalink to &quot;Run standalone using docker compose&quot;">​</a></h1><p>If you are running only Home Assistant Core, or do not have Home Assistant Supervisor, you might want to run this addon as a standalone service. Docker Compose is commonly used to manage services that run in docker containers.</p><p>As a standalone service this addon does not depend on Home Assistant Core, Supervisor or OS. This means you can run this addon and send data to any MQTT server, without using any other HA services.</p><p>Another benefit of this setup is to run this addon along with <code>mbusd</code> on a Raspberry Pi without having to install Home Assistant on it.</p><h2 id="base-image" tabindex="-1">Base image <a class="header-anchor" href="#base-image" aria-label="Permalink to &quot;Base image&quot;">​</a></h2><p>In these example commands we prefix the <code>docker-compose build</code> commands with the environment variable definition <code>BUILD_FROM=...</code>, which specifies which base image is used. For a Raspberry Pi you would need to use <code>BUILD_FROM=homeassistant/armhf-base-python:3.9</code>, and for a 64bit PC you would use <code>BUILD_FROM=homeassistant/amd64-base-python:3.9</code>. A list of available base images can be found in <code>hass-addon-sunsynk-multi/build.yaml</code> and <code>hass-addon-mbusd/build.yaml</code>. Use the one that is most appropriate for your host computer.</p><h2 id="sunsynk-multi" tabindex="-1">Sunsynk Multi <a class="header-anchor" href="#sunsynk-multi" aria-label="Permalink to &quot;Sunsynk Multi&quot;">​</a></h2><ul><li>Copy <code>options.yaml.template</code> to <code>options.yaml</code> and make changes to <code>options.yaml</code> to match your setup. Use <code>PORT: tcp://mbusd:502</code> for the inverter port if you want to use the <code>mbusd</code> included in this docker compose stack.</li><li>Build the image <code>BUILD_FROM=&lt;base_image&gt; docker compose build sunsynk-multi</code></li><li>Run the container <code>docker compose up -d sunsynk-multi</code></li><li>See the container logs <code>docker compose logs -f sunsynk-multi</code></li></ul><h2 id="mbusd" tabindex="-1">Mbusd <a class="header-anchor" href="#mbusd" aria-label="Permalink to &quot;Mbusd&quot;">​</a></h2><ul><li>Edit <code>docker-compose.yaml</code> changing the values under <code>environment</code> to match your configuration, leaving the device set to <code>/dev/ttyUSB0</code> as we mount the correct port to this location in the next step.</li><li>Under <code>volumes</code> change <code>/dev/ttyRS485</code> to the RS485 port of your host computer.</li><li>Build the image <code>BUILD_FROM=&lt;base_image&gt; docker compose build mbusd</code></li><li>Run the container <code>docker compose up mbusd</code></li><li>View container logs <code>docker compose logs -f mbusd</code></li></ul>',10),d=[n];function i(c,r,l,u,m,h){return t(),o("div",null,d)}const y=e(s,[["render",i]]);export{b as __pageData,y as default};
